@model List<AlwahaLibrary.Models.MenuItem>

<div class="row g-3">
    @foreach (var item in Model)
    {
        <div class="col-12 col-md-6 col-lg-4 mx-3">
            <div class="card shadow-sm h-100">
                <div class="card-header py-3" style="background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);">
                    <h5 class="card-title mb-0 text-white fw-bold">@item.Name</h5>
                </div>
                <div class="card-body d-flex flex-column">
                    <p class="card-text flex-grow-1">@item.Description</p>
                    <p class="mb-0 fs-5 fw-bold text-primary">£@item.Price</p>
                </div>
            </div>
        </div>
    }
</div>